"""The agent layer.

The model reasons; the tools act. Nothing here lets a model reach the database
directly -- it may only ask, by name and with arguments this package validates,
for one of a fixed set of operations that already exist elsewhere in the
application.
"""
