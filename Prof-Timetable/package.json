{
  "name": "profschedule-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered personal timetable and reminder system for college professors — Python/FastAPI backend, deployed on Vercel.",
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
